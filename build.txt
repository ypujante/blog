Building the blog and starting a server:
jekyll --server --url http://localhost:4000

Building the blog only (somehow starting the server takes forever)
jekyll --url http://localhost:4000

Building for deployment:
jekyll

